{
  "name": "mdmaker",
  "version": "1.0.0",
  "description": "builds the markdown files based on claims.json",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npm start --silent -- './__testdata__/TestArticle'",
    "start": "node --input-type=module --eval 'const { appendConfiguration } = await import(\"./index.js\"); const [markdown, author] = process.argv.slice(1); appendConfiguration(`${markdown}`, author);'"
  },
  "author": "NiRUS",
  "license": "ISC"
}